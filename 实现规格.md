# 贴吧相册下载器 — 完整实现规格（可直接交给任何模型写）

> 本规格记录**已实测验证通过**的路线。旧版"以浏览器渲染 HTML 为权威源"的 DOM 抓取路线已废弃（图片只抓到 1~3 张），现行路线是"**浏览器只用于相册列表页，图片/评论全部走 JSON 接口**"。

## 1. 产品形态与运行方式
- Windows 图形界面 EXE（tkinter），双击运行，可分享。
- 粘贴贴吧相册链接（或贴吧名）→ 登录一次 →「开始保存」→ 实时进度 → 一键打开输出目录。
- 离线 HTML 快照（含评论）+ 全部图片按原顺序、按 `序号_描述.ext` 命名保存到本地。

## 2. 目录结构要求（硬性）
```
输出目录/贴吧名称/相册列表.html           # 离线列表页（卡片+封面+图片数）
输出目录/贴吧名称/相册列表_原始快照.html   # 列表页原始 DOM 存档（排错）
输出目录/贴吧名称/某相册.html             # 每本相册离线页（含评论，图指本地）
输出目录/贴吧名称/static/                # 离线 CSS
输出目录/贴吧名称/manifest.json           # 机器可读清单（顺序/描述/源URL/本地路径/评论）
输出目录/贴吧名称/某相册/图片/001_描述.jpg # 图片按原顺序
```

## 3. 技术路线（实测验证通过）

| 步骤 | 方式 | 是否需登录 |
|------|------|-----------|
| 相册列表页 | **真实浏览器**（Playwright / CDP 直连 Edge）渲染 `tab=album`，从 DOM 解析相册 tid+名 | **需要**（未登录被安全验证拦，解析到 0 本） |
| 图片清单 | HTTP JSON 接口 `picture/list`（ps/pe 分页拉全） | 不需要 |
| 原图下载 | `https://imgsa.baidu.com/forum/pic/item/<pic_id>.jpg`（无 cookie 可用） | 不需要 |
| 评论 | HTTP JSON：图片详情页 `/photo/p?pic_id=...&pn=N` 内嵌 `albumData`，按 pn 翻页 | 不需要 |

**关键结论**：贴吧对 curl / 无头 CLI（`--dump-dom` 也试过）一律返回「百度安全验证」拦截页，只有相册列表页需要真实浏览器；图片与评论接口本身不需要登录，但必须带正确 UA + `Referer: https://tieba.baidu.com/`。

## 4. 关键实测情报（模板可直接用）

### 4.1 相册列表页（唯一需浏览器的步骤）
- URL：`https://tieba.baidu.com/f?kw={kw}&ie=utf-8&tab=album`
- 解析（DOM，lxml）：相册 tid 来自 `/p/<tid>` 链接，相册名来自 `.grbm_ele_title a`。
- 列表页 HTML 中数据常被包在 `<!--...-->` 注释里：先 `re.sub(r"<!--(.*?)-->", r"\1", html, re.S)` 揭开，再用 lxml 解析；若仍解析不到，把 `#` 替换成 `"` 再试（贴吧注释怪癖），最后用正则兜底抓 `/p/<tid>`。
- **必须登录**：实测未登录（全新 profile）解析到 0 本；已登录（持久化 profile）解析到 26 本。

### 4.2 图片清单接口（核心，无需浏览器）
```
GET https://tieba.baidu.com/photo/g/bw/picture/list
    ?kw={kw}&alt=jview&rn=200&tid={tid}&pn=1&ps=1&pe=200&info=1
```
- **全站 GBK 编码**：响应 `json.loads(raw.decode('gb18030'))`，不是 UTF-8（否则中文乱码）。
- `data.pic_list[]` 每条：`index`（0 起，顺序）、`pic_id`（40 位 hex）、`descr`（图片描述=最接近"原名"的字段）、`purl`/`murl`（缩放图 URL）、`original_url`（原图）。
- **分页**：用 `ps`/`pe` 区间（每页 200）拉全。实测某相册 `ps=1,pe=200` / `201,400` / `401,600` / `601,708` = 708 张，`index` 连续 0..707。**`pic_id` 游标分页不可靠，用区间分页。**
- 顺序由 `index` 保证；数量以 `data.total_num` 为准（偶比实际多 1，属服务端含已删图）。

### 4.3 原图下载
- 优先 `https://imgsa.baidu.com/forum/pic/item/<pic_id>.jpg`（实测可用，无 cookie）；
- 失败回退 `purl` → `murl`（清单里的缩放图 URL）。
- 必须带 `Referer: https://tieba.baidu.com/`，否则被防盗链挡。
- 文件头校验：`\xff\xd8\xff`(jpg) / `\x89PNG`(png) / `GIF8`(gif) / `RIFF....WEBP`(webp)；非图片判失败。扩展名按文件头嗅探，不盲信 URL 后缀（实测有 `.gif` 实际是 gif）。

### 4.4 评论接口（无需浏览器）
```
GET https://tieba.baidu.com/photo/p?kw={kw}&ie=utf-8&flux=1&tid={tid}&pic_id={40位}&pn=1&fp=2&see_lz=1
```
- 返回服务端渲染 HTML，内含 `var albumData = {...};`（**GBK**）。解析 `albumData`：
  - `comment_amount`：评论总数（用于对照）。
  - `comment_list[]`：每条 `{floor, user_name, content, time}`，`content` 里可能嵌 `<img class="BDE_Smiley">` 表情，需保留为文字或图片。
  - `desc`：图片描述；`img.original.url` / `img.medium.url`：图片 URL。
- **翻页**：`pn=2..N`，每页 10 条，直到取到 `comment_amount` 条。实测某图 33 条 / 3 页。
- 解析注意：`albumData` 用单引号包裹值，正则提取需 `re.sub(r"'", '"', seg)` 后 `json.loads`（或手写提取 key/value）。

### 4.5 离线 HTML 快照
- 自写模板（仿贴吧视觉网格），不依赖现网 CSS：`<img src>` 指向本地 `./<相册名>/图片/<序号_描述.ext>`。
- rel_url 对每段做 `quote` 百分号编码（中文、`#`、`%` 在 `file://` 下也能打开）。
- 评论嵌在对应图片下方；索引页 `相册列表.html` 列出每本相册卡片（封面+图片数），链接 `./<相册名>.html`。
- **硬要求**：生成的 HTML 不得残留任何 `imgsa.baidu.com/forum/pic/item` 之类远程图片地址（自检项）。

### 4.6 manifest.json
记录 `kw`、抓取时间、每本相册（`name`/`tid`/`total_num`/`saved`/`failed`、每张图 `order`/`pic_id`/`descr`/`file`/`ok`/`bytes`/`ext`/`comment_total`/`comment_count`/`comments`）。支持断点续传：重跑时复用已存在且 >1KB 的图片、复用已抓到的评论（`prev.get("comments")`）。

## 5. 命名规则（已与用户确认）
- 格式 `序号_描述.ext`：序号 3 位补零（`001_`），描述取 `descr` 经 `sanitize()`（去掉 `\/:*?"<>|` 及控制符，截断 60 字），扩展名按文件头嗅探。
- 无 `descr` → 回落 `pic_id`（40 位 hex）。
- 同名 → 追加 `(2)`/`(3)`。
- 理由：贴吧全站不保存上传者原始文件名，只有 `pic_id` 和 `descr`；`descr` 是红警3吧里"0将军刽子手0""动画版 by 真空菊爆器"这类真实文字。

## 6. 登录流程
- 浏览器打开 `https://tieba.baidu.com/`，用户登录；轮询 `BDUSS` cookie 存在即成功（~6 分钟超时），会话持久化到 `LOCALAPPDATA/贴吧相册下载器/profile`。
- 浏览器后端抽象 `BrowserBackend`：`start() / get_html(url, settle_ms, scroll, max_scroll_rounds) / is_logged_in() / open_login(url, timeout) / close()`。
- 两种实现：
  - **PlaywrightBackend**：`launch_persistent_context(channel="msedge"→"chrome"→内置Chromium 回退)`，有头，复用系统 Edge。
  - **CdpBackend（瘦身版）**：`subprocess` 启动系统 Edge `--remote-debugging-port=`，`websocket-client` 走 CDP 取 DOM；`start()` 后先落 `tieba.baidu.com` 建立 cookie 上下文再 `getCookies`。
- `default_browser()`：能 `import playwright` 用 playwright，否则自动用 cdp。

## 7. GUI 结构（app.py, tkinter）
- `App(tk.Tk)`：链接框、输出目录选择（默认 `D:\贴吧相册备份`、每次弹框）、「登录百度账号」「开始保存」「停止」「打开输出目录」、评论勾选 + 每图最多 N 条、并发数、滚动日志、进度条。
- 跨线程：工作线程 daemon + `queue.Queue`，主线程 `after(120)` 轮询 `_drain` 更新控件；停止用 `threading.Event`。
- 窗口程序看不到 traceback：未捕获异常写 `启动错误.log`（exe 旁）；`--selftest` 结果写 `selftest_result.txt`。

## 8. 自测（离线，不联网）
`python crawler.py --selftest`，7 项全过：GBK 解码 / ps-pe 分页拉全 / 扩展名嗅探 / 同名不覆盖 / 相册列表注释解析 / albumData 评论解析 / 离线 HTML 无残留远程地址且含评论。

## 9. 打包（PyInstaller onedir）
- **Playwright 版** `tieba_album_getter.spec`：`console=False`；`hiddenimports += collect_submodules("playwright")+collect_submodules("tbalbum")+["crawler","app"]`；`collect_data_files/collect_dynamic_libs("playwright")` 收 driver（复用系统 Edge，不打包 Chromium）；产物 `dist\贴吧相册下载器\` ~137MB。
- **CDP 瘦身版** `tieba_album_getter_slim.spec`：`excludes=["playwright",...]`；`hiddenimports += collect_submodules("tbalbum")+collect_submodules("websocket")+collect_submodules("lxml")+["crawler","app"]`；产物 `~15MB`，要求目标机已装 Edge/Chrome。
- 构建注意：本环境 `rm -rf`/PyInstaller `--clean` 会触发 safe-delete 报错（沙箱回收站不可用），改用**重命名旧目录 + 不带 `--clean`** 构建。
- `build.bat`：pip 装依赖（playwright、lxml、pyinstaller）→ pyinstaller spec。Python 3.10.11。

## 10. 当前已修复的 bug（v1 根因，已解决）
- 图片只抓 1~3 张：原 v1 用浏览器渲染 HTML 取图，因 `lxhtml` 变量名写错静默失败，退化到兜底只拿 1~2 张。现行 v2 走 `picture/list` JSON 接口，实测 1609/1611（差 2 张是服务端 `total_num` 含已删图）。
- 文件名为 40 位 hash：改为 `序号_描述.ext`。
- GBK 乱码：所有贴吧接口按 `gb18030` 解析。
- 评论为空：改为 `/photo/p` 的 `albumData` 分页解析，实测每图条数与 `comment_amount` 一致。

## 11. 交付物清单
`app.py`、`crawler.py`、`tbalbum/`（http, naming, album_api, photo_api, downloader, manifest, snapshot, album_list, browser/base+playwright_backend+cdp_backend+__init__）、`tieba_album_getter.spec`、`tieba_album_getter_slim.spec`、`build.bat`、`requirements.txt`、`README.md`、本文件。
